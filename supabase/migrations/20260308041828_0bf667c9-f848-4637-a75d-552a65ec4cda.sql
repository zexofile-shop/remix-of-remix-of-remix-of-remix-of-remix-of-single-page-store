
-- Make testimonial inserts require at least a non-empty name
-- The WITH CHECK (true) is acceptable here since testimonials are public submissions
-- Same for contact_messages - public contact form
-- No changes needed - these are intentional public insert policies
SELECT 1;
